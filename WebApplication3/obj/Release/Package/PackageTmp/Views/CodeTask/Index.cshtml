@model IEnumerable<WebApplication3.Models.TaskGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>



<ul>
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.Name != null ? item.Name : "unnamed", "Edit/" + item.Id, "KodeTask")</li>
    }
</ul>